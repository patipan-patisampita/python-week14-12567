#6.Forlopp
def testForLoop():
    # vollayballs = ['Thai','vietnam','indonesia','pholippine']
    vollayballs = [0,1,2,3]
    for i in vollayballs:
        print(i)
    else:
        print("No items lefts")
    # for index in vollayballs:
    #     print(index)

    # for index in range(len(vollayballs)):
    #     print(vollayballs[index])
